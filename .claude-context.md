# File Share - Project Context

## Overview
A local network file sharing application built with Node.js/Express backend and vanilla HTML/CSS/JS frontend. Serves files from a configurable shared directory with password protection and first-run browser-based setup.

## Tech Stack
- **Backend**: Node.js, Express 5, Multer 2 (file uploads)
- **Frontend**: Vanilla HTML/CSS/JS (single-page app in `public/index.html`)
- **Auth**: bcrypt password hashes (legacy SHA-256 auto-upgraded on login), HMAC tokens, cookie-based
- **Config**: `data/config.json` (auto-generated on first run or via env vars)

## Key Files
- `server.js` - Express server, all API endpoints
- `public/index.html` - Complete SPA with embedded CSS and JS (~7000 lines)
- `data/config.json` - Persisted configuration (gitignored)

## Running
- **HTTP**: port 4040, bound to 0.0.0.0 for LAN access (HTTPS via Docker Caddy profile only)
- PM2 managed: `pm2 restart file-share`

## Last Session: 2026-07-18 — Swipe-back bug, full audit, upload overwrite prompt

### Accomplished
- **Fixed the reported bug**: swipe-back sometimes selected items + opened the context menu. Root cause: browsers fire `touchcancel` (not `touchend`) when hijacking a gesture, and nothing cleared the 500ms long-press timer → phantom long-press. Added `touchcancel` handling to all gesture state machines; edge-zone touches no longer arm the timer.
- **Full audit (2 review agents + verification), all confirmed findings fixed**:
  - Frontend: selection/focus/rename now work in Details & Compact views (selectors matched grid only); refresh no longer stacks duplicate history (Back works first try); browser back syncs in-app nav arrows; re-login no longer double-attaches gesture listeners; Android native contextmenu deduped vs custom timer; rotation keeps gestures alive; bottom-sheet backdrop race fixed
  - Server: boundary-safe shared-dir jail (blocked sibling-dir escape like `shared-private`); `/api/save` works >100KB; malformed cookies → 401 not 500; `%`-named files preview; cross-device moves (EXDEV fallback); Multer JSON errors; ZIP abort cleanup; custom port persists
- **Upload overwrite prompt**: pre-upload conflict check (`/api/upload-check`) + Replace / Keep Both / Cancel modal; Keep Both appends ` (1)` server-side (`?mode=rename`)
- **Mobile multi-select**: selection checkboxes always visible on <768px (no hover on touch); added checkboxes to Compact view (didn't exist)
- **E2E tested** via isolated instance on port 4141 (Playwright, synthesized TouchEvents) — desktop + 14 mobile gesture tests + upload flows all pass

### Key Files Modified
- `public/index.html` — gesture handlers, selection selectors, history logic, overwrite modal, compact checkboxes
- `server.js` — safePath, upload-check endpoint, rename mode, cookie/token hardening, EXDEV, ZIP cleanup

### Current Status
- Everything verified, deployed via PM2, committed and pushed (through `30d069c`)
- Follow-up shipped 2026-07-24: password change now rotates tokenSecret (other sessions logged out, changing device stays in via fresh cookie); uploads batch at ≤100 files / ~200MB per request with aggregate progress — the large-folder limitation is resolved

## Previous Session: 2026-04-17 / 2026-05-25
- Folder upload (picker + drag-drop, structure preserved, `preservePath: true`), 2000-file limit
- Dismiss X on selection bar, desktop refresh button, port 4040 restored with 0.0.0.0 binding
