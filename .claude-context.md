# File Share - Project Context

## Overview
A local network file sharing application built with Node.js/Express backend and vanilla HTML/CSS/JS frontend. Serves files from a configurable shared directory with password protection and first-run browser-based setup.

## Tech Stack
- **Backend**: Node.js, Express, Multer (file uploads)
- **Frontend**: Vanilla HTML/CSS/JS (single-page app in `public/index.html`)
- **Auth**: Password hashed with SHA-256 + random salt, HMAC-based tokens with optional expiry
- **Config**: `data/config.json` (auto-generated on first run or via env vars)
- **HTTPS**: mkcert self-signed certs in `certs/` (gitignored), covers localhost, 192.168.1.44, 100.64.146.99, x570d4u

## Key Files
- `server.js` - Express server, all API endpoints, HTTPS/HTTP setup
- `public/index.html` - Complete SPA with embedded CSS and JS
- `data/config.json` - Persisted configuration (gitignored)
- `certs/` - mkcert TLS certificates (gitignored)

## Running
- **HTTPS**: `https://x570d4u:4443` (primary), `https://192.168.1.44:4443`, `https://100.64.146.99:4443`
- **HTTP redirect**: Port 4040 redirects to HTTPS on 4443
- **CA cert download**: `http://<host>:4040/ca.crt` for installing on new devices
- PM2 managed: `pm2 restart file-share`

## Last Session: 2026-04-17 — Folder uploads

### Accomplished
- **Folder upload via button**: New "📁 Folder" button (desktop toolbar, mobile menu, FAB) opens a native folder picker (`<input webkitdirectory>`)
- **Folder upload via drag-drop**: Dropping a folder now recurses via `webkitGetAsEntry()` and uploads with structure preserved
- **Drop anywhere**: Full-page drop now works outside the center dropzone; only `Files`-type drops trigger upload so internal drags aren't hijacked
- **Upload limit bump**: 50 → 2000 files per request
- **Bug fixes**:
  - Multer was silently stripping directory parts from filenames — fixed with `preservePath: true` (the actual reason folders flattened)
  - ID collision: my new folder picker used `id="folderInput"`, same as the existing "New Folder" dialog text input, which broke `createFolder()`. Renamed to `folderUploadInput`
  - Double-fire between dropzone inline `ondrop` and document-level drop listener — added `stopPropagation` and removed flat-file fallback that masked the bug

### Key Files Modified
- `server.js` — New `sanitizeRelativePath()` helper, multer `destination` callback does `mkdir -p` for nested uploads and re-validates via `safePath()`, `preservePath: true`, file limit bumped to 2000
- `public/index.html` — New `readEntry()`/`handleDataTransfer()` functions, `uploadFiles()` sends `webkitRelativePath` as multipart filename, folder picker input + buttons, document drop handler accepts drops anywhere

### Current Status
- All changes working, **uncommitted**
- Known limitation: single request per upload — browsers choke on very large folders (>~2 GB or thousands of files). Batched/chunked uploads not yet implemented; user flagged this as a future improvement.

## Previous Session: 2026-03-16
- HTTPS support (mkcert certs, port 4443 with HTTP→HTTPS redirect, /ca.crt endpoint)
- Click-to-select / double-click-to-open (Explorer-style)
- Path persistence on page refresh via `window.location.hash`
