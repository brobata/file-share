# File Share - Project Context

## Overview
A local network file sharing application built with Node.js/Express backend and vanilla HTML/CSS/JS frontend. Serves files from a configurable shared directory with password protection and first-run browser-based setup.

## Tech Stack
- **Backend**: Node.js, Express, Multer (file uploads)
- **Frontend**: Vanilla HTML/CSS/JS (single-page app in `public/index.html`)
- **Auth**: Password hashed with SHA-256 + random salt, HMAC-based tokens with optional expiry
- **Config**: `data/config.json` (auto-generated on first run or via env vars)
- **HTTPS**: mkcert self-signed certs in `certs/` (gitignored), covers localhost, 192.168.1.44, 100.64.146.99, x570d4u

## Key Files
- `server.js` - Express server, all API endpoints, HTTPS/HTTP setup
- `public/index.html` - Complete SPA with embedded CSS and JS
- `data/config.json` - Persisted configuration (gitignored)
- `certs/` - mkcert TLS certificates (gitignored)

## Running
- **HTTPS**: `https://x570d4u:4443` (primary), `https://192.168.1.44:4443`, `https://100.64.146.99:4443`
- **HTTP redirect**: Port 4040 redirects to HTTPS on 4443
- **CA cert download**: `http://<host>:4040/ca.crt` for installing on new devices
- PM2 managed: `pm2 restart file-share`

## Last Session: 2026-03-16 — HTTPS, click-to-select, path persistence

### Accomplished
- **HTTPS support**: Added mkcert TLS certs, server.js serves HTTPS on port 4443 with HTTP→HTTPS redirect on 4040
- **CA cert endpoint**: `http://<host>:4040/ca.crt` serves rootCA for easy install on other devices
- **Click-to-select / double-click-to-open**: Changed from single-click-opens to Explorer-style single-click=select, double-click=open across all view modes (grid, details, compact)
- **Path persistence on refresh**: App reads `window.location.hash` on load to restore current directory instead of resetting to defaultDir
- **Extracted `openItem()` function**: Consolidated open logic (navigate/preview/download) used by dblclick, Enter key, and context menu
- **Ops Cockpit HTTPS**: Set up HTTPS proxy for ops-command-center on port 3443 using same mkcert certs

### Key Files Modified
- `server.js` — Added `https`/`http` imports, HTTPS server with cert loading, HTTP redirect with CA cert endpoint
- `public/index.html` — New `openItem()`/`handleDblClick()` functions, single-click selects, hash-based path restore in `initApp()`
- `.gitignore` — Added `certs/`

### Current Status
- All changes working and tested, **uncommitted**
- Cert covers: localhost, 127.0.0.1, 192.168.1.44, 100.64.146.99, x570d4u
- Cert expires: June 2028

## Previous Session: 2026-02-24
- 20 Windows Explorer features (batches 1-7) + 14 mobile-native features
- All committed and pushed
